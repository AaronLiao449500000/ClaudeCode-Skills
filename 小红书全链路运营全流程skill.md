# Skill 名称
小红书全链路运营全流程

# Skill 作用
【纯调度器版本，100%复用本地已有Skill】用户只需要输入赛道关键词，本Skill作为调度器自动按流程调用本地所有独立Skill完成「赛道分析→自动选品→冷启动规划→选题池规划→多平台内容生成」全链路运营工作，每个环节支持用户审核修改，确认后自动进入下一环，最终输出三平台完整运营方案。

本Skill仅做流程调度和参数传递，所有业务逻辑全部调用本地已有独立Skill，不重复开发任何功能，最大化复用现有资源：
1. 只需一个指令即可自动调度所有环节，无需逐个调用单个Skill
2. 自动在各Skill之间传递参数，无需手动复制粘贴上游输出结果
3. 每个环节输出都可审核修改，修改后的内容自动传给下游Skill
4. 所有输出自动按规范保存到对应目录，方便后续使用
5. 完全兼容所有单个Skill的独立调用，原有使用习惯不变

---

# 适用场景
适用于以下任务：
- 从零开始做一个小红书新账号，需要完成全套前期准备工作
- 想快速测试一个新赛道的可行性，从分析到内容产出一站式完成
- 希望标准化小红书运营流程，减少人工重复操作
- 不需要逐个调用单个Skill，想要一次性获得完整运营方案

---

# 输入格式
用户仅需要提供核心参数，其他参数会自动从上游环节结果中提取：
- 赛道关键词（必填）
- 目标人群（选填，不填则由赛道分析环节自动识别）
- 客单价范围（选填，不填默认20-200元）
- 账号模式（选填，默认带货号）
- 规划周期（选填，默认30天）
- 生成内容数量（选填，默认生成选题池前10篇内容）
- 目标平台（选填，可选值：小红书/公众号/视频号/全平台，默认仅小红书）
- 内容分发节奏（选填，可选值：错峰发布/同步发布，默认错峰发布）
- 公众号内容长度（选填，默认1500-2500字）
- 视频号时长（选填，默认60秒）
- 实时搜索（选填，默认开启，可设置为关闭以提升运行速度）

# 调用示例
## 单平台极简调用（默认仅小红书，兼容旧版本）
```
赛道：儿童学习习惯
```

## 全平台调用（生成三平台全套内容）
```
赛道：儿童学习习惯
目标人群：小学家长
客单价：20-100元
目标平台：全平台
内容分发节奏：错峰发布
生成内容：10组
```

---

# 整体工作流
本 Skill 严格按照以下顺序执行，每个环节完成后自动暂停等待用户审核，审核通过后进入下一环：

## 流程总览
```
用户输入赛道关键词（可选择目标平台）
↓
环节1：赛道分析 → 输出【三平台赛道分析报告】→ 用户审核（可修改）→ 确认进入下一环
↓
环节2：自动选品 → 自动读取赛道分析报告数据 → 输出选品报告 → 用户审核（可修改）→ 确认进入下一环
↓
环节3：冷启动规划 → 自动读取前两个环节报告数据 → 输出【三平台统一冷启动规划】→ 用户审核（可修改）→ 确认进入下一环
↓
环节4：选题池规划 → 自动读取前三个环节报告数据 → 输出【三平台统一内容日历】→ 用户审核（可修改）→ 确认进入下一环
↓
环节5：内容生成 → 自动读取选题池内容 → 批量生成小红书图文内容 → 用户审核（可修改）→ 确认进入下一环
↓
环节6：跨平台内容适配（可选，根据用户选择的目标平台自动触发）
   ├─ 自动生成公众号深度长文
   ├─ 自动生成视频号成片脚本
   ↓
全流程完成，输出三平台全套内容方案
```

---

## 环节1：赛道分析执行规则【调用本地Skill】
1. 自动使用用户输入的赛道、目标人群、客单价、实时搜索等参数，**调用本地独立Skill文件：【赛道分析skill.md】**
2. 所有参数自动传递给赛道分析Skill，本调度器不处理任何业务逻辑
3. 分析深度默认使用「标准」，用户自定义参数自动传递
4. 自动接收赛道分析Skill的输出结果
5. 输出报告自动保存到：`AI应用博主赛道运营方案/01-赛道分析/[赛道名称]-三平台赛道分析报告.md`
6. 审核阶段：
   - 用户可以直接修改报告内容
   - 选择「通过」：自动提取报告中的核心参数，作为参数传给下一个环节的Skill
   - 选择「重跑」：重新调用本地赛道分析Skill生成新报告
   - 选择「终止」：结束整个流程

---

## 环节2：自动选品执行规则【调用本地Skill】
1. 自动从赛道分析报告中提取以下参数，无需用户重复输入：
   - 赛道名称
   - 目标人群
   - 核心痛点
   - 机会点
   - 推荐起号方向
   - 不建议方向
2. 将提取的参数自动传递，**调用本地独立Skill文件：【小红书自动选品skill.md】**
3. 选品深度默认使用「标准」，用户自定义参数自动传递
4. 自动接收选品Skill的输出结果
5. 输出报告自动保存到：`AI应用博主赛道运营方案/02-自动选品/[赛道名称]-选品报告.md`
6. 审核阶段：
   - 用户可以直接修改选品清单和商品信息
   - 选择「通过」：自动提取推荐商品列表、内容方向等参数，传给下一个环节的Skill
   - 选择「重跑」：重新调用本地自动选品Skill生成新报告
   - 选择「终止」：结束整个流程

---

## 环节3：冷启动规划执行规则【调用本地Skill】
1. 自动从前面两份报告中提取以下参数：
   - 赛道
   - 目标人群
   - 商品方向（从选品报告的推荐商品中提取）
   - 核心痛点
   - 推荐内容方向
   - 用户焦虑点
   - 趋势机会点
2. 结合用户输入的账号模式、冷启动周期等参数，自动传递，**调用本地独立Skill文件：【小红书账号冷启动与发文规划skill.md】**
3. 发文频率默认「隔天1篇」，新手模式默认开启，参数自动传递
4. 自动接收冷启动规划Skill的输出结果，自动适配三平台要求
5. 输出报告自动保存到：`AI应用博主赛道运营方案/03-冷启动规划/[赛道名称]-三平台冷启动规划.md`
6. 审核阶段：
   - 用户可以直接修改规划内容
   - 选择「通过」：自动提取账号定位、发文频率、风格要求等参数，传给下一个环节的Skill
   - 选择「重跑」：重新调用本地冷启动规划Skill生成新报告
   - 选择「终止」：结束整个流程

---

## 环节4：选题池规划执行规则【调用本地Skill】
1. 自动从前面三份报告中提取以下参数：
   - 赛道
   - 目标人群
   - 账号阶段：默认「冷启动」
   - 商品方向
   - 内容目标：默认「收藏+评论+轻种草」
   - 规划周期：用户输入的周期，默认30天
   - 发文频率：从冷启动规划中提取
   - 风格方向：从冷启动规划中提取
   - 目标平台：用户输入的目标平台
   - 分发节奏：用户输入的分发节奏
2. 将所有参数自动传递，**调用本地独立Skill文件：【小红书选题池与内容日历规划.md】**
3. 默认区分内容类型（流量型/信任型/互动型/种草型），自动适配三平台选题
4. 自动接收选题池Skill的输出结果
5. 输出报告自动保存到：`AI应用博主赛道运营方案/04-选题池规划/[赛道名称]-三平台统一内容日历.md`
6. 审核阶段：
   - 用户可以直接修改选题和日历安排
   - 选择「通过」：自动提取选题列表，传给下一个环节的Skill
   - 选择「重跑」：重新调用本地选题池规划Skill生成新报告
   - 选择「终止」：结束整个流程

---

## 环节5：小红书内容生成执行规则【调用本地Skill】
1. 自动从选题池报告中提取前N个选题（N为用户输入的生成数量，默认10篇）
2. 自动提取以下公共参数：
   - 赛道
   - 目标人群
   - 风格要求：默认「第一人称、口语化、低AI痕迹、适度emoji」
   - 内容形式：默认「图文」
   - 图片页数量：默认5页
   - 是否需要标题候选和评论区互动：默认是
3. 循环逐篇将参数传递，**调用本地独立Skill文件：【小红书单篇内容生成skill.md】**
4. 自动接收每篇内容的输出结果
5. 每篇内容自动保存到：`AI应用博主赛道运营方案/05-内容生成/小红书/[序号]-小红书-[选题名称].md`
6. 审核阶段：
   - 用户可以逐篇修改内容
   - 单篇内容支持重跑，重新调用内容生成Skill
   - 所有内容审核完成后，自动进入下一环节

---

## 环节6：跨平台内容适配执行规则【调用本地Skill，可选触发】
1. 如果用户选择了「全平台」「公众号」「视频号」任意一个，自动触发本环节
2. 自动读取已生成的小红书内容作为母版，根据目标平台分别调用对应的本地Skill：
   - ✅ 需要生成公众号内容：逐篇调用本地独立Skill文件：【公众号深度长文生成Skill.md】，自动传递参数
   - ✅ 需要生成视频号内容：逐篇调用本地独立Skill文件：【视频号成片脚本生成Skill.md】，自动传递参数
3. 适配完成后自动保存：
   - 公众号内容保存到：`AI应用博主赛道运营方案/05-内容生成/公众号/[序号]-公众号-[选题名称].md`
   - 视频号脚本保存到：`AI应用博主赛道运营方案/05-内容生成/视频号/[序号]-视频号-[选题名称].md`
4. 审核阶段：
   - 用户可以逐篇修改各平台内容
   - 单篇支持重跑，重新调用对应平台的Skill
   - 所有内容审核完成后，全流程结束

---

# 数据流转规则
1. 所有上游环节的输出报告都会被自动读取，不需要用户手动传递参数
2. 每个环节只需要补充上一环节没有的特殊参数，大部分参数自动继承
3. 如果用户在环节审核时修改了报告内容，修改后的内容会自动作为下一环节的输入
4. 所有数据都保存在本地文件中，不会上传到外部服务

---

# 文件保存规则
所有输出文件统一保存到「AI应用博主赛道运营方案」目录下对应的子目录：
- 01-赛道分析：保存【三平台赛道分析报告】
- 02-自动选品：保存选品报告
- 03-冷启动规划：保存【三平台统一冷启动规划】
- 04-选题池规划：保存【三平台统一内容日历】
- 05-内容生成/小红书：保存小红书图文内容
- 05-内容生成/公众号：保存公众号深度长文
- 05-内容生成/视频号：保存视频号成片脚本

文件命名统一规则：`[赛道名称]-[报告类型].md`，内容文件命名：`[序号]-[平台]-[选题名称].md`

---

# 审核逻辑说明
本Skill采用全环节审核机制，每个环节完成后都会暂停，给用户提供三个操作选项：
1. ✅ 通过：确认当前环节结果，自动进入下一个环节
2. 🔄 重跑：重新执行当前环节，生成新的结果
3. ✏️ 修改：用户可以直接编辑当前环节的输出内容，修改后的内容会作为后续环节的输入
4. ❌ 终止：直接结束整个流程，保留已经生成的所有文件

---

# 输出说明
全流程完成后，你将获得以下完整产出（根据选择的目标平台）：
1. 【三平台通用】完整的赛道分析报告（含各平台特性评估）
2. 【三平台通用】可落地的选品清单（带供应链和内容方向建议）
3. 【三平台通用】30天统一冷启动运营规划（可直接执行）
4. 【三平台通用】30天统一内容日历（标注各平台发布时间）
5. 【小红书】前N篇完整图文笔记（可直接发布）
6. 【公众号】前N篇深度长文（可直接发布）
7. 【视频号】前N套成片脚本（可直接导入AI工具生成视频）

所有文件都已经按分类保存到对应目录，可直接使用。

---

# 📢 纯调度器版本声明
本Skill是**100%纯流程调度器**，不内置任何业务逻辑，所有环节的执行逻辑全部调用你本地已有的独立Skill文件：
| 执行环节 | 调用的本地Skill文件 |
|---------|---------------------|
| 赛道分析 | 【赛道分析skill.md】 |
| 自动选品 | 【小红书自动选品skill.md】 |
| 冷启动规划 | 【小红书账号冷启动与发文规划skill.md】 |
| 选题池规划 | 【小红书选题池与内容日历规划.md】 |
| 小红书内容生成 | 【小红书单篇内容生成skill.md】 |
| 公众号长文生成 | 【公众号深度长文生成Skill.md】 |
| 视频号脚本生成 | 【视频号成片脚本生成Skill.md】 |
| 单独跨平台适配 | 【跨平台内容一键适配Skill.md】 |

✅ 你可以随时修改本地独立Skill的逻辑，调度器会自动使用修改后的最新版本，不需要更新调度器本身
✅ 所有单个Skill的独立调用方式完全不变，和之前的使用习惯100%兼容
✅ 完全不浪费你已有的Skill资源，所有现有Skill都可以复用

---

# 质量检查清单
每个环节执行时都会自动遵守对应单个Skill的质量要求，全流程额外检查：
- 各环节参数传递是否正确
- 文件是否按规则保存到正确路径
- 审核环节是否正常触发
- 后续环节是否正确使用用户修改后的内容
- 所有输出是否符合小红书平台规则和运营最佳实践

---

# 使用示例
用户输入：
```
/skill 小红书全链路运营全流程 赛道：儿童学习习惯
```

Skill执行过程：
1. 自动生成儿童学习习惯赛道分析报告 → 你审核确认
2. 自动基于分析报告生成选品清单 → 你审核确认
3. 自动生成30天冷启动规划 → 你审核确认
4. 自动生成30天选题池和内容日历 → 你审核确认
5. 自动生成前10篇内容文案 → 你审核完成，获得全套运营方案
