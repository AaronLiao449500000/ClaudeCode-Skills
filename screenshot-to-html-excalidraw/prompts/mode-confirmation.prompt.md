# mode-confirmation.prompt.md

每次复刻前必须先确认输出模式，不允许直接开始生成。

固定询问话术建议：

> 在开始复刻前，请先确认输出方式：
> 1. 只生成 HTML
> 2. 只生成 Excalidraw
> 3. HTML 和 Excalidraw 都生成
> 请直接回复数字或对应文字。

规则：
- 只有在用户已经明确确认模式时，才可以跳过这一步
- 如果用户表述模糊，也必须先问
- 不能默认“两个都生成”，除非用户已明确要求
