# Excalidraw Font Notes

这个仓库默认把 Excalidraw 的字体控制拆成两层：

1. 场景文件本身负责可编辑布局
2. Obsidian CSS snippet 负责显示字体体验

原因：

- Excalidraw 排版不是网页 CSS 盒模型
- 不同环境下字体渲染不完全一致
- 在 Obsidian 中，CSS snippet 是更直接的显示控制方式

默认推荐中文使用：
- `LXGW WenKai Screen / 霞鹜文楷屏幕阅读版`
