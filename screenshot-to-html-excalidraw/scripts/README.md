# scripts/README.md

这个仓库当前以 Skill 文档、Prompt 和模板为主。

如果后续需要扩展自动化脚本，建议优先添加：

1. `build_excalidraw_md.py`
   - 将结构化布局信息转成 `layout.excalidraw.md`

2. `select_font_snippet.py`
   - 根据用户选择复制对应 CSS snippet

3. `assemble_outputs.py`
   - 根据输出模式组装最终产物

目前本仓库已经具备作为 Skill 仓库上传 GitHub 的完整文档结构。
